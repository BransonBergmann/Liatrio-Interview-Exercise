name: send_message docker image build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build: 

    runs-on: ubuntu-latest

    steps: 
    - uses: actions/checkout@v4
    - name: builds docker image
      run: docker build --tag sendMessage:$(date +%s) .